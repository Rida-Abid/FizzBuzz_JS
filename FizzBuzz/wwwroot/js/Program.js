var arr = [];
arr = prompt("Enter your numbers").split("");
function Main(arr)
{
  

        if (arguments.length != 3) {
            console.error("Please enter 3 values");
            console.log("Usage: fizzId, buzzId, maxCount");
        }
        else {
            var fizzId, buzzId, maxCount = arr[0]; [1], [2];
            fizzId = parseInt(fizzId);
            buzzId = parseInt(buzzId);
            maxCount = parseInt(maxCount);
            const logic = new Logic()
            var results = logic.Process(fizzId, buzzId, maxCount);
            result.foreach(Process)
            console.log(result);
            


                
        }
     

}
